// Stub for basehub/next-image
import NextImage from "next/image";
import * as React from "react";

export const BaseHubImage = (props: React.ComponentProps<typeof NextImage>) => {
  return <NextImage {...props} />;
};
